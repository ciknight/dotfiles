Ag
==
* `-l` only shows the files (not the occurrences themselves)
* `-Q` parses as a literal instead of regexp (e.g. `ag -Q .rb` for references to ruby files, not for strings with rb preceded by stuff)
* `-i` is case-insensitive. `-s` is case-sensitive.


